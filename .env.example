# Environment Configuration
NODE_ENV=production
PORT=3000

# Database
DATABASE_PATH=./backend/database.sqlite

# JWT Authentication (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_jwt_secret_here

# Admin Credentials
ADMIN_USERNAME=admin
# Generate hash with: node -e "import('bcryptjs').then(bcrypt => bcrypt.hash('your_password', 10).then(console.log))"
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here

# CORS (your production domain)
ALLOWED_ORIGIN=https://nepaliurbandictionary.com
